VERY experimental core. 
will load .bin files from within retroarch. 

NOTE . I can't get .S files to even be recognized as a valid type, passing via commandline will work if you're in the right path. 

Launch with 
retroarch.exe -v --log-file log.txt -L minicube_libretro.so <path to bin>


